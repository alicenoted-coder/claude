# 在 Vercel 專案的 Settings → Environment Variables 設定以下變數
# （本機若要用 `vercel dev` 測試，可複製成 .env.local）

# 你的通關密語 — 自己決定，別人不知道就進不來
APP_PASSPHRASE=請改成你自己的密語

# 以下兩個由 Vercel KV / Upstash 整合自動帶入，通常不用手動填
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
